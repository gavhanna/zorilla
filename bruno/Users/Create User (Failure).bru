meta {
  name: Create User
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/users
  body: json
  auth: none
}

body:json {
  {
    "name": "New User",
    "email": "newuser@example.com"
  }
}

assert {
  res.status: eq 401
}
