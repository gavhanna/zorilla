meta {
  name: Create Recording
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/recordings
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "My Recording",
    "duration": 120
  }
}

assert {
  res.status: eq 200
}
