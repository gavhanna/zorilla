meta {
  name: Get Recording By ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/recordings/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}
