# ===================================================================
# Zorilla Docker Environment Configuration
# ===================================================================
# Copy this file to .env and configure your settings

# -------------------------------------------------------------------------------
# Database (External - User Managed)
# -------------------------------------------------------------------------------
# You must provide your own PostgreSQL database. Set one of these:

# Option 1: Full connection string (recommended)
DATABASE_URL=postgresql://zorilla:your-password@your-db-host:5432/zorilla

# Option 2: Individual components (alternative)
POSTGRES_USER=zorilla
POSTGRES_PASSWORD=your-password
POSTGRES_DB=zorilla
POSTGRES_HOST=your-db-host
POSTGRES_PORT=5432

# -------------------------------------------------------------------------------
# Application Configuration
# -------------------------------------------------------------------------------
# Port to expose the application on
APP_PORT=5000

# Directory for audio file storage (absolute path or relative to docker-compose.yml)
DATA_DIR=./data

# -------------------------------------------------------------------------------
# Security
# -------------------------------------------------------------------------------
# JWT Secret - generate a strong random string!
JWT_SECRET=changeme-to-a-strong-random-string

# Enable/disable new user registration
ENABLE_REGISTRATION=true

# -------------------------------------------------------------------------------
# Admin User (created on first startup only)
# -------------------------------------------------------------------------------
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme
ADMIN_NAME=Admin

# -------------------------------------------------------------------------------
# Transcription
# -------------------------------------------------------------------------------
# Model: tiny, base, small, medium, large-v1, large-v2, large-v3
WHISPER_MODEL_NAME=base

# Enable transcription worker
TRANSCRIPTION_WORKER_ENABLED=true

# Polling interval in milliseconds
TRANSCRIPTION_WORKER_INTERVAL_MS=2000

# Transcription timeout in milliseconds (5 minutes default)
TRANSCRIPTION_TIMEOUT_MS=300000
